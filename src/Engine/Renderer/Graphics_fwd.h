#pragma once

struct Matrix4;

class Image;
class McFont;
class Shader;
class RenderTarget;
class VertexArrayObject;
class Graphics;

struct TextFX;

enum class AnchorPoint : unsigned char;
enum class DrawPrimitive : unsigned char;
enum class DrawUsageType : unsigned char;
enum class DrawPixelsType : unsigned char;
enum class MultisampleType : unsigned char;
enum class TextureWrapMode : unsigned char;
enum class TextureFilterMode : unsigned char;
enum class DrawBlendMode : unsigned char;
enum class DrawCompareFunc : unsigned char;
