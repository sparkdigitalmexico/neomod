# pins wheel routing on layered scrollviews (flipped at P2.3, was the "UBER hack" pin):
# with the skin dropdown (a UIContextMenu, i.e. a scrollview nested inside
# options_contents) open and hovered, one wheel event produces exactly ONE consumer and
# it is the top-most scroll surface, the DROPDOWN (options_contextmenu) -- even though
# its content fits (no scrolling enabled), so the page must NOT scroll beneath it (the
# golden tail has no content-movement hover churn). pre-2.3, update order won over
# z-order and the parent consumed. needs the UITestSkin fixture (run.sh creates it).
options_save_on_back 0
debug_fixed_frametime 0.0166667
ui_trace 1
mouse_raw_input 0
mouse_sensitivity 1
ui_scale_to_dpi 0
windowed_resolution 1280x720
mouse_to 10 10
@wait 30
mouse_to 640 360
@wait 2
mouse_down
@wait 3
mouse_up
@wait 40
mouse_to 867 401
@wait 3
mouse_down
@wait 3
mouse_up
@wait 30
mouse_to 40 267
@wait 3
mouse_down
@wait 3
mouse_up
@wait 20
mouse_to 256 492
@wait 3
mouse_down
@wait 3
mouse_up
@wait 2
mouse_to 10 10
@wait 300
ui_assert visible options_contextmenu 1
mouse_to 193 492
@wait 3
mouse_wheel -1
@wait 5
exit
